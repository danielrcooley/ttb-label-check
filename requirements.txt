# Runtime dependencies, pinned.
# rapidocr declares a dependency on the desktop `opencv-python` wheel, which needs display libraries
# (libGL, X11) even in a container. We install rapidocr with --no-deps (see requirements-ocr.txt) and
# list its runtime dependencies here with the headless OpenCV wheel instead.
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
pydantic==2.13.5
pydantic-settings==2.15.0
numpy==2.5.2
opencv-python-headless==5.0.0.93
pillow==12.3.0
rapidfuzz==3.14.6
onnxruntime==1.29.0
# rapidocr runtime dependencies (kept in sync with `pip show rapidocr`)
colorlog==6.10.1
omegaconf==2.3.0
pyclipper==1.4.0
PyYAML==6.0.3
requests==2.32.5
shapely==2.1.2
six==1.17.0
tqdm==4.67.1
