Unable to produce a trustworthy review: the workspace policy rejected every read-only shell command before execution, including `rg --files` and `Get-Content`. Consequently, I could not read the seven requested sections or inspect the code and line numbers. No files were modified.

Please rerun with read-only shell execution enabled, or provide the repository contents directly.