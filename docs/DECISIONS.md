# Decision log

Dated, in the order they were made. Each entry says what was decided, why, and what evidence or
review changed it. Decisions marked **(corrected)** record a first proposal that was wrong and
how it was caught. Owner of every decision: Daniel R. Cooley. Builder: Claude Code. Reviewer: Codex.

| ID | Date | Decision | Why | Evidence / source |
|---|---|---|---|---|
| D-001 | 2026-09-03 | Python + FastAPI backend, vanilla HTML/JS frontend styled with the U.S. Web Design System, one container. | Primary language of the author; strongest OCR ecosystem; one URL; no build step; no CDN so it works behind a restrictive firewall; USWDS is the federal standard and Section 508 ready. | Brief: Marcus on firewall; Sarah on simplicity. |
| D-002 | 2026-09-03 | OCR runs in-process. No LLM or cloud vision API in the core verification path. Pluggable provider interface for an optional, off-by-default second opinion. | The brief's firewall note and 5-second limit rule cloud calls out; determinism and pixel-traceable findings matter for a government audit tool. | Brief: Marcus, Sarah. Review 001 agreed. |
| D-003 | 2026-09-03 | The tool recommends; the agent decides. No auto-approve or auto-reject anywhere. | Dave's "you need judgment"; federal AI guidance requires human oversight for rights-impacting decisions. | Brief: Dave. |
| D-004 | 2026-09-03 | No Treasury or TTB seals. Prototype banner instead. Configurable agency branding slot left empty in the public demo. | Federal law restricts use of Treasury symbols in ways implying endorsement; the reviewers' own seal in the invitation email is a signature graphic, not a project asset. | Discussion with author. |
| D-005 | 2026-09-03 | RapidOCR (ONNX Runtime) is the sole engine; Tesseract dropped. Final configuration: bundled PP-OCRv6 small detector and recognizer, angle classifier off, detector side 640, input 1280 px, recognizer batch 16, one intra-op thread per engine. Models vendored with SHA-256. | Measured on 46 synthetic images: RapidOCR recovered ABV 90-100% and net contents 90-100% across degradations; Tesseract was 3x faster but lost small text (ABV 76%, net 71%) and failed on sideways images. Among RapidOCR configurations, the bundled small models matched or beat the v4 and English models on accuracy at 40-50% less single-thread time, and they need no download at all. | docs/BAKEOFF.md, docs/OCR_EVAL.md (runs A-H). |
| D-006 | 2026-09-03 | Preprocessing is minimal: EXIF transpose, downscale, 90-degree retry on low confidence. **(corrected)** | The plan had deskew, CLAHE, glare suppression and zoom passes. Measurement showed the engine already handles glare, blur, perspective, low contrast and small text; only sideways images failed. | docs/BAKEOFF.md; Review 001 3.2. |
| D-007 | 2026-09-03 | Pin Python 3.12 in the container and CI; build the lock on linux/amd64; vendor model files with SHA-256; no downloads at build or run. **(corrected)** | On Python 3.13 pip silently resolved the OCR library to a 2023 release whose thread settings were ignored, which produced misleading concurrency numbers. | Review 001 4.2 and 4.3; perf run. |
| D-008 | 2026-09-03 | A COLA application is a set of images (front, back, neck), merged before checks. **(corrected)** | First plan assumed one image per application; the warning is usually on the back label, so that design would flag every front label as missing the warning. | Author review pass 2. |
| D-009 | 2026-09-03 | Primary input is label artwork; photographs are a best-effort secondary tier reported separately. **(corrected)** | Brief says agents look at "label artwork"; Jenny calls photos "maybe out of scope". | Author review pass 3. |
| D-010 | 2026-09-03 | Application fields are accepted as strings exactly as written (e.g. "45% Alc./Vol. (90 Proof)"); the same parser runs on both sides; comparison is numeric. **(corrected)** | The brief's sample shows string fields; a bare numeric field would have failed the sample. | Author review pass 3. |
| D-011 | 2026-09-03 | Warning check: exact match is the only Pass. Near matches are Needs review with a character diff. Anchor caps and bold, body not bold, contrast and size are Needs review findings with evidence. | 27 CFR 16.21 text and 16.22 format verified from the eCFR API; Jenny: "It has to be exact." Review 001 1.4 tightened an earlier "OCR-tolerant" design. | docs/REGULATIONS.md; Review 001. |
| D-012 | 2026-09-03 | Heuristic findings never produce a failure; only clear text or numeric mismatches do. | Keeps the false-alarm rate low (Dave's phone-system story) and leaves judgment with the agent. | Author review pass 5. |
| D-013 | 2026-09-03 | Bottler name/address and country of origin are core comparisons when supplied; origin is required when the application is marked imported. **(corrected)** | The brief lists both as required label elements; the plan had demoted them to optional. | Review 001 1.6, 6.7. |
| D-014 | 2026-09-03 | Batch pairing defaults to explicit filenames or a CSV column; fuzzy pairing is a confirmable suggestion, never automatic. **(corrected)** | Back labels often carry no brand, ABV or net contents, so automatic pairing could attach them to the wrong application with confidence. | Review 001 2.2. |
| D-015 | 2026-09-03 | One deployment host (Azure Container Apps) plus a tested local Docker command; a second host is an emergency plan, not a parallel deployment. **(corrected)** | Two hosts doubled operational surface for a one-week prototype; Azure domains also have better odds of being reachable from a government network. | Review 001 2.1. |
| D-016 | 2026-09-03 | One priority-aware capacity limiter; cooperative pass budgets instead of pretend timeouts; streaming size caps; tmpfs for temporary files. **(corrected)** | Two semaphores could exceed CPU; a deadline cannot stop a running inference; multipart can spool to disk. | Review 001 2.4, 2.5, 2.8. |
| D-017 | 2026-09-03 | Standards of fill checked against the application's stated net contents only, Needs review only. | Real agents check container sizes; using application data removes OCR risk; the lists changed in January 2025 so a hard failure would be wrong. | docs/REGULATIONS.md; Review 001 3.4 (narrowed rather than cut). |
| D-018 | 2026-09-03 | Acceptance gate for speed: deployed, end-to-end, front+back application p95 under 5 seconds under concurrent batch load. | The authors bolded the 5-second sentence; a per-image number on a laptop is not the same claim. | Brief emphasis; Review 001 1.1. |
| D-019 | 2026-09-03 | AI assistance is disclosed openly: commit trailers kept, Claude Code listed under tools used, this log records where the AI's first proposal was wrong. | The role is AI-directed engineering and the panel knows the workflow; the direction and correction are the work product. | Author. |
| D-020 | 2026-09-03 | OCR workers are a thread pool, one engine per thread with one ONNX intra-op thread each, N = vCPUs. Process pool kept only as a crash-isolation option. **(corrected twice)** | First perf run showed no scaling, which turned out to be the old library ignoring thread settings. With real controls, 2 threads gave 1.84x and 4 gave 3.1x the single-worker throughput, and a process pool gave identical numbers (0.79 vs 0.79 img/s at 2 workers; 1.33 vs 1.31 at 4). Threads are simpler and share memory. | docs/OCR_EVAL.md runs D, T, P. |
| D-021 | 2026-09-03 | Per-image single-core cost is about 2.4 s on a laptop core, so the 5-second application gate needs at least 2 real vCPUs and the two images of an application must run in parallel. Hosting is chosen for dedicated CPU and measured cost, not theme. | Measured (run D). Azure Container Apps always-on at 2 vCPU costs roughly $150/month after the first month's credit; dedicated 2-vCPU machines on Fly or Hetzner cost $8-62/month. Decision on host deferred to the author with this data. | docs/OCR_EVAL.md; provider price lists (to be confirmed at signup). |
