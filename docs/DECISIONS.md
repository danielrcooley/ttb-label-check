# Decision log

Dated, in the order they were made. Each entry says what was decided, why, and what evidence or
review changed it. Decisions marked **(corrected)** record a first proposal that was wrong and
how it was caught. Owner of every decision: Daniel R. Cooley. Builder: Claude Code. Reviewer: Codex.

| ID | Date | Decision | Why | Evidence / source |
|---|---|---|---|---|
| D-001 | 2026-09-03 | Python + FastAPI backend, vanilla HTML/JS frontend styled with the U.S. Web Design System, one container. | Primary language of the author; strongest OCR ecosystem; one URL; no build step; no CDN so it works behind a restrictive firewall; USWDS is the federal standard and Section 508 ready. | Brief: Marcus on firewall; Sarah on simplicity. |
| D-002 | 2026-09-03 | OCR runs in-process. No LLM or cloud vision API in the core verification path. Pluggable provider interface for an optional, off-by-default second opinion. | The brief's firewall note and 5-second limit rule cloud calls out; determinism and pixel-traceable findings matter for a government audit tool. | Brief: Marcus, Sarah. Review 001 agreed. |
| D-003 | 2026-09-03 | The tool recommends; the agent decides. No auto-approve or auto-reject anywhere. | Dave's "you need judgment"; federal AI guidance requires human oversight for rights-impacting decisions. | Brief: Dave. |
| D-004 | 2026-09-03 | No Treasury or TTB seals. Prototype banner instead. Configurable agency branding slot left empty in the public demo. | Federal law restricts use of Treasury symbols in ways implying endorsement; the reviewers' own seal in the invitation email is a signature graphic, not a project asset. | Discussion with author. |
| D-005 | 2026-09-03 | RapidOCR (ONNX Runtime) is the sole engine; Tesseract dropped. Final configuration: bundled PP-OCRv6 small detector and recognizer, angle classifier off, detector side 640, input 1280 px, recognizer batch 16, one intra-op thread per engine. Models vendored with SHA-256. | Measured on 46 synthetic images: RapidOCR recovered ABV 90-100% and net contents 90-100% across degradations; Tesseract was 3x faster but lost small text (ABV 76%, net 71%) and failed on sideways images. Among RapidOCR configurations, the bundled small models matched or beat the v4 and English models on accuracy at 40-50% less single-thread time, and they need no download at all. | docs/BAKEOFF.md, docs/OCR_EVAL.md (runs A-H). |
| D-006 | 2026-09-03 | Preprocessing is minimal: EXIF transpose, downscale, 90-degree retry on low confidence. **(corrected)** | The plan had deskew, CLAHE, glare suppression and zoom passes. Measurement showed the engine already handles glare, blur, perspective, low contrast and small text; only sideways images failed. | docs/BAKEOFF.md; Review 001 3.2. |
| D-007 | 2026-09-03 | Pin Python 3.12 in the container and CI; build the lock on linux/amd64; vendor model files with SHA-256; no downloads at build or run. **(corrected)** | On Python 3.13 pip silently resolved the OCR library to a 2023 release whose thread settings were ignored, which produced misleading concurrency numbers. | Review 001 4.2 and 4.3; perf run. |
| D-008 | 2026-09-03 | A COLA application is a set of images (front, back, neck), merged before checks. **(corrected)** | First plan assumed one image per application; the warning is usually on the back label, so that design would flag every front label as missing the warning. | Author review pass 2. |
| D-009 | 2026-09-03 | Primary input is label artwork; photographs are a best-effort secondary tier reported separately. **(corrected)** | Brief says agents look at "label artwork"; Jenny calls photos "maybe out of scope". | Author review pass 3. |
| D-010 | 2026-09-03 | Application fields are accepted as strings exactly as written (e.g. "45% Alc./Vol. (90 Proof)"); the same parser runs on both sides; comparison is numeric. **(corrected)** | The brief's sample shows string fields; a bare numeric field would have failed the sample. | Author review pass 3. |
| D-011 | 2026-09-03 | Warning check: exact match is the only Pass. Near matches are Needs review with a character diff. Anchor caps and bold, body not bold, contrast and size are Needs review findings with evidence. | 27 CFR 16.21 text and 16.22 format verified from the eCFR API; Jenny: "It has to be exact." Review 001 1.4 tightened an earlier "OCR-tolerant" design. | docs/REGULATIONS.md; Review 001. |
| D-012 | 2026-09-03 | Heuristic findings never produce a failure; only clear text or numeric mismatches do. | Keeps the false-alarm rate low (Dave's phone-system story) and leaves judgment with the agent. | Author review pass 5. |
| D-013 | 2026-09-03 | Bottler name/address and country of origin are core comparisons when supplied; origin is required when the application is marked imported. **(corrected)** | The brief lists both as required label elements; the plan had demoted them to optional. | Review 001 1.6, 6.7. |
| D-014 | 2026-09-03 | Batch pairing defaults to explicit filenames or a CSV column; fuzzy pairing is a confirmable suggestion, never automatic. **(corrected)** | Back labels often carry no brand, ABV or net contents, so automatic pairing could attach them to the wrong application with confidence. | Review 001 2.2. |
| D-015 | 2026-09-03 | One deployment host (Azure Container Apps) plus a tested local Docker command; a second host is an emergency plan, not a parallel deployment. **(corrected)** | Two hosts doubled operational surface for a one-week prototype; Azure domains also have better odds of being reachable from a government network. | Review 001 2.1. |
| D-016 | 2026-09-03 | One priority-aware capacity limiter; cooperative pass budgets instead of pretend timeouts; streaming size caps; tmpfs for temporary files. **(corrected)** | Two semaphores could exceed CPU; a deadline cannot stop a running inference; multipart can spool to disk. | Review 001 2.4, 2.5, 2.8. |
| D-017 | 2026-09-03 | Standards of fill checked against the application's stated net contents only, Needs review only. | Real agents check container sizes; using application data removes OCR risk; the lists changed in January 2025 so a hard failure would be wrong. | docs/REGULATIONS.md; Review 001 3.4 (narrowed rather than cut). |
| D-018 | 2026-09-03 | Acceptance gate for speed: deployed, end-to-end, front+back application p95 under 5 seconds under concurrent batch load. | The authors bolded the 5-second sentence; a per-image number on a laptop is not the same claim. | Brief emphasis; Review 001 1.1. |
| D-019 | 2026-09-03 | AI assistance is disclosed openly: commit trailers kept, Claude Code listed under tools used, this log records where the AI's first proposal was wrong. | The role is AI-directed engineering and the panel knows the workflow; the direction and correction are the work product. | Author. |
| D-020 | 2026-09-03 | OCR workers are a thread pool, one engine per thread with one ONNX intra-op thread each, N = vCPUs. Process pool kept only as a crash-isolation option. **(corrected twice)** | First perf run showed no scaling, which turned out to be the old library ignoring thread settings. With real controls, 2 threads gave 1.84x and 4 gave 3.1x the single-worker throughput, and a process pool gave identical numbers (0.79 vs 0.79 img/s at 2 workers; 1.33 vs 1.31 at 4). Threads are simpler and share memory. | docs/OCR_EVAL.md runs D, T, P. |
| D-021 | 2026-09-03 | Per-image single-core cost is about 2.4 s on a laptop core, so the 5-second application gate needs at least 2 real vCPUs and the two images of an application must run in parallel. Hosting is chosen for dedicated CPU and measured cost, not theme. | Measured (run D). First estimate of Azure Container Apps at ~$150/month assumed a fully busy container; the idle rate that applies most of a review window puts a 2 vCPU / 4 GiB always-on app nearer $45-60/month, inside the $200 first-month credit. Dedicated 2-vCPU machines on Fly or Hetzner cost $8-62/month. **(corrected)** cost estimate. | docs/OCR_EVAL.md; provider price lists (to be confirmed at signup). |
| D-022 | 2026-09-03 | The warning report carries an explicit `assessment` (exact, case, noise, wording, absent) and the verdict keys off it: exact passes; case and noise are Needs review; wording and absent are issues. Similarity thresholds no longer decide. | Thresholds blurred the one distinction that matters to Jenny: OCR noise versus a changed word. Word-level classification is explainable in one sentence. | Evaluation run 1 on the synthetic corpus; `tests/unit/test_warning.py`. |
| D-023 | 2026-09-03 | Accents in the OCR read of the warning are removed before the literal comparison; a single-character difference inside a word of five or more letters, and digit/letter confusables including `1`/`i`, count as noise. (Superseded: D-029 for accents; D-035 and D-039 for what exactness ignores.) | The recognizer (a multilingual model) emits "alcoholič" and "drivé" for plain letters; the regulation text has no accented letters, so an accent is recognition noise by definition. Real wording changes replace whole words. **(corrected)** after evaluation run 1 showed clean artwork failing the warning check. | `docs/EVAL.md` history; `tests/unit/test_warning.py`. |
| D-024 | 2026-09-03 | Reading order groups lines into rows by vertical overlap (at least half the shorter line's height), then sorts left to right. | Height-bucket sorting scrambled labels with mixed text sizes or tight line spacing, which truncated the warning span. **(corrected twice)**: first version divided by each line's own height, second by fixed buckets. | `tests/unit/test_match.py::test_reading_order_uses_vertical_overlap_not_height_buckets`. |
| D-025 | 2026-09-03 | Batch-mode requests (`X-Batch: 1`) read their images one at a time; interactive requests fan out in parallel. | A batch request is refused rather than queued when no slot is free, so a two-image batch request that already held one slot was being refused halfway through, which the load test exposed as a 429 storm. **(corrected)** | `docs/LOADTEST.md`; `tests/integration/test_batch_mode.py`. |
| D-026 | 2026-09-03 | The browser batch client limits concurrent requests, not concurrent applications, sizes the pool from `/health`, and backs off with growing delays on 429. The server reserves nothing statically; batch may use every slot unless a person is waiting. | The first design (one reserved slot, item-level concurrency) starved batch on a two-worker host and still produced refusals. Measured in the browser smoke test. **(corrected)** | `app/static/batch.js`; `tests/unit/test_pool.py`. |
| D-027 | 2026-09-03 | Host: Azure Container Apps (2 vCPU / 4 GiB, minReplicas 1), account created on deploy day so the 30-day credit covers the review window; a neutral custom domain fronts it so the submitted URL survives a later move to another host. | Their cloud and the best odds of being reachable from a government network, at no cost for the window that matters; the domain removes the risk of a dead link if hosting changes. | Author decision after the cost correction. |
| D-028 | 2026-09-04 | CI's container job must run its check with stdin attached and fail when the app is not ready; warm-up failures are surfaced in health and readiness. Shell scripts on this machine lose backslashes, so every multi-line edit goes through a script file. **(corrected)** | The first container job passed while running nothing (no `-i` on `docker exec`) and the app never became ready because system libraries were missing; an origin regex had literal backspace bytes from the same shell behavior. Both were caught by the Codex code review and by reading the raw job log. | Review 002; CI runs 33818820324, 33819136086. |
| D-029 | 2026-09-04 | Exact means exact: accents in the OCR read of the warning are noise (Needs review), not stripped before the literal comparison. **(corrected)** (Refined by D-035 and D-039: exact ignores letter case and spacing next to punctuation, nothing else.) | I had stripped accents to keep clean artwork at "exact"; the reviewer was right that a legal status must not paper over an instrument's uncertainty. The evaluation reports the resulting false alarms honestly. | Review 002 item 1.6. |
| D-030 | 2026-09-04 | When the label images of one application state different alcohol contents, that contradiction is a mismatch in itself, regardless of which one agrees with the application. | A front label at 40% and a back label at 45% cannot both be right; preferring the one that matches the application would hide the defect. Percent statements now also need an alcohol-by-volume marker so "5% alcohol flavoring" is not parsed as a statement. | Review 002 item 1.5; integration test wrong_abv. |
| D-031 | 2026-09-04 | A worker slot is held until the OCR thread finishes, even when the client disconnects; batch requests hold one slot for all their images. **(corrected)** | Releasing on cancellation oversubscribed the CPU; per-image admission could refuse a multi-image batch request halfway. Both pinned by unit tests. | Review 002 items 1.1, 1.2. |
| D-032 | 2026-09-04 | The recognizer decodes with its alphabet restricted to printable ASCII, for every line, as a configuration of the OCR engine (`TTB_OCR_ASCII_ALPHABET`, default on). "Exact" keeps its meaning: one transcript, compared literally. **(author's decision, against the reviewer's ranking)** | The multilingual model emitted stray accented letters on clean artwork (2 of 10 labels), turning exact warnings into Needs review. Review 003 ranked "do nothing" first and warned against overriding an accent-capable read with a second opinion; it also proposed a native-resolution re-read, which I tested and which did not converge (a different accent appeared, and a previously exact label lost a letter). Restricting the alphabet is not a second opinion: US labels are English text and the regulation is pure ASCII, so it is the same choice as using an English recognizer, with none of the latency. Genuinely accented print is read as its base letter; comparisons already fold accents and the evidence crop shows the print. | docs/reviews/003-accents.*; experiment in the decision record; evaluation re-run in docs/EVAL.md. |
| D-033 | 2026-09-04 | Large JPEGs are decoded at reduced size (libjpeg scaling by 2, 4 or 8, never below the working size); the canonical size and the box scale come from the file header and its EXIF orientation, never from the decoded image. **(corrected)** | The reduced-size decode added after review 002 recorded the decoded size as the image size, so boxes and evidence crops would have been in the wrong coordinate space whenever it engaged. It was dormant only because its 2x margin could not engage below the 25 MP limit; lowering `TTB_OCR_MAX_SIDE` would have exposed it. Found in the late-session self-review; pinned by two unit tests (plain and EXIF-rotated). The decode now engages for 12 MP phone photos, which cost a quarter of the memory and time. | tests/unit/test_images.py; docs/reviews/004-late-session.*. |
| D-034 | 2026-09-04 | Ready requires every check to be complete: only the bottler and country-of-origin checks may be "not checked", and only because the application omitted them. A required value the tool cannot interpret is Needs review. The alphabet wrapper fails closed and renormalizes confidence; the image no longer asserts proxy trust; nothing is published unless the test job passed. **(corrected)** | Review 004 found that an uninterpretable net-contents value could ride through to Ready, that the ASCII wrapper would silently fall back to the full alphabet on a shape change, that the container image trusted X-Forwarded-For wherever it ran, and that the container job could publish while tests failed. The alcohol comparison also treated a proof on its own line as a competing percent. All fixed with tests; the alphabet restriction itself (D-032) stays, with its false-pass direction now stated in the limits. | docs/reviews/004-late-session.*; tests/unit/test_parsers.py, test_alphabet.py, test_security.py. |
| D-035 | 2026-09-04 | Three changes from the first real-label sample (150 approved COLAs from TTB's Public COLA Registry, `docs/EVAL_REAL.md`): (a) "exact" for the warning ignores letter case and spacing and nothing else, because 16.22 regulates capitals only for the two anchor words and most approved labels print the whole statement in capitals; the anchor's capitals stay a separate format check; (b) when no usable warning is found upright, the images are re-read sideways and only a warning span found that way is kept, because small labels print the statement vertically along one edge of an otherwise upright label (how many re-reads, when, and for which requests is D-039); (c) the bottler comparison folds "Bottled by" style prefixes and corporate forms (Company/Co., LLC, Inc.) on both sides, and the fuzzy review threshold drops from 90 to 80, because real labels abbreviate and decorative fonts read at 80-89 while the genuinely different names in the sample scored under 70. **(corrected)** | On the first 39 real records the warning was never "exact": 15 were all-capitals bodies and most of the rest were capitals plus one OCR slip; six "absent" warnings included a 50 mL label with the statement printed vertically; applicant names matched 0 of 39 because the registry spells out "Green Cheek Beer Company" where the label prints "BREWED BY GREEN CHEEK BEER CO."; two brand misses were decorative fonts scoring 86 and 89. The synthetic corpus, being generated, had none of these and would never have shown them. Each change is pinned by a unit or integration test; both evaluations were re-run. | docs/EVAL_REAL.md; tests/unit/test_warning.py, tests/unit/test_parsers.py; tests/integration/test_verify_api.py (sideways warning). |
| D-036 | 2026-09-04 | The warning finder's "same column" filter measures overlap across the text direction, so lines read from a rotated image (vertical strips once mapped back) are joined into one statement. **(corrected)** | The filter compared horizontal overlap in the original image's coordinates; for a sideways photo, or a label printing the statement vertically, every line after the anchor was rejected as "another column" and the statement came back as a one-line fragment. This is why the sideways tier of the synthetic evaluation reported two issues, and it made the new sideways re-read useless until found by its test. Pinned by a unit test with vertical boxes, a unit test of the re-read with a fake engine, and an integration test with a sideways back label. | tests/unit/test_warning.py; tests/unit/test_services.py; tests/integration/test_verify_api.py. |
| D-037 | 2026-09-04 | From hand-checking real records against their images: (a) the anchor may be split over two lines ("GOVERNMENT" set large, "WARNING:" beneath it); (b) large artwork whose statement is unreadable at the working size is read once more at bounded full resolution, only when no usable statement was found; (c) the noise classifier tolerates one slip (dropped, added, changed or two swapped letters) in words of four or more letters, hyphenated line breaks in capitals, and a bare number swept in from a neighbouring line, all still Needs review with the diff. A dropped or replaced word stays a wording issue. **(corrected)** | An absinthe label printed the anchor on two lines and was reported "absent"; a round 3 L label printed the statement in small type on a 2,930-pixel image and read as fragments; approved labels read as "Suregon", "YOURS", "CONSUMP- TION" and "26 (2)" were small print, not wording. One approved tequila label genuinely prints "WOMAN" and "OF RISK OF BIRTH DEFECTS": the tool now reports the first as review and the missing word as an issue, which is the right answer under the word-for-word rule. Both evaluations re-run. | docs/EVAL_REAL.md hand-checked cases; tests/unit/test_warning.py, tests/unit/test_services.py, tests/unit/test_normalize.py. |
| D-038 | 2026-09-04 | Within a reading-order row, lines whose left edges sit within half a line's thickness of each other share a column and stay top to bottom; only genuinely separate columns are ordered left to right. **(corrected)** | Tight body text in a narrow column produces OCR boxes that overlap vertically, so consecutive statement lines were grouped as one "row" and sorted by their left edge; three pixels of edge jitter swapped two lines, and the joined statement read as "wording" with the same words deleted and re-inserted. Eight of the 42 wording verdicts on the real sample were this. Pinned by a unit test; both evaluations re-run. | tests/unit/test_warning.py (tight column); docs/EVAL_REAL.md. |
| D-039 | 2026-09-04 | From review 005 and the measurements of the deployed real-label build: (a) the warning re-read runs for interactive requests only, as one extra round of reads in parallel across the workers, at most one read per worker: the images turned 270 then 90 degrees (270 unrolls a statement printed up the right edge, which three of the four such labels in the real sample do) and, for large artwork, once at full resolution (that read first when the heading was seen but the body was not); the orientations the rotation retry already read are used first and cost nothing; batch requests read every image exactly once. (b) "Exact" for the warning compares the sequence of words and punctuation marks, ignoring letter case and spacing next to punctuation, so a word boundary moved between letters ("womens hould") can never pass. (c) A bottler match whose corporate forms differ on the two sides ("LLC" against "Inc.") is Needs review. (d) In the statement finder a vertical strip and a horizontal line are never one column, and a rescued statement replaces what the kept read had in the same place. **(corrected)** | Measured on the deployed build: the batch screen reads each image before it knows its application, so every front label (no statement by design) paid three extra reads and single-image throughput fell from 0.98 to 0.23 images per second; a front-only upload took 8.6 s against a five-second requirement (`docs/LOADTEST.md`, af9e239 rows). The reviewer showed that deleting every space let "womens hould" pass as exact, that folding "LLC" and "Inc." away could pass two different legal entities, and that the re-read's outcome depended on server load. The fake-engine tests for the new round exposed (d): the strips of a sideways statement crossing an upright label's brand line were spliced together. A bound on reads was preferred to a clock budget because a clock would make the verdict depend on the machine's speed. | `docs/reviews/005-real-labels.*`; `docs/LOADTEST.md`; `tests/unit/test_services.py`, `test_warning.py`, `test_parsers.py`. |
| D-040 | 2026-09-04 | Net contents may be left blank in the application (form and spreadsheet). A blank is Needs review: the result shows the net contents read from the label with its crop and asks the agent to confirm it; it can never be Ready on its own. Alcohol content was already optional; brand, class/type and beverage type stay required. | Hands-on testing with the real registry corpus: TTB's COLA form (F 5100.31) carries no net contents field, so a spreadsheet exported from real applications had every row rejected ("net_contents: Field required") and the batch screen then showed the wrong message ("Add images first", fixed in the same change). The brief's sample application lists net contents, so the comparison stays exactly as it was whenever a value is given. | `tests/unit/test_parsers.py` (blank net contents), `tests/unit/test_csvio.py`; `docs/EVAL_REAL.md` (the form's fields). |
| D-041 | 2026-09-04 | Which findings may fail an application, decided against real application data (consult 006): (a) the bottler check takes the registered line apart (trade name, legal name, the name marked as used on the label, city, state, ZIP), tries the whole line and each name folded, and uses the city and state as corroboration: name and address read is a match, name only is Needs review, a different name on the label's own "bottled by" line or nothing resembling the name is Needs review with the reason; (b) class/type is Match when the text agrees and otherwise Needs review, never an issue; (c) an alcohol statement that is required but was not read, and a country of origin that was not read, are Needs review; an origin statement naming a different country, and numeric disagreements in alcohol or net contents, stay issues; (d) brand and the warning are unchanged; (e) from the first hands-on look at the result panels: a registered name printed whole inside the label's bottler line, with its city and state, is a match rather than a "close match"; when nothing on the label resembles the class description no "closest text" is shown; and digits from a barcode read glued onto a word of the warning statement ('or' read as 'OR88 186"223932') are noise for review, not a wording issue. | 146 real applications with a spreadsheet built from the registry's own fields came back 144 "Issues found": the registered bottler line (two names and a street address) never matched a label that prints "BREWED BY GREEN CHEEK BEER CO." and a city (141 rows), and the registry's class descriptions ("TABLE WHITE WINE") never match the designation a label prints ("Pinot Grigio") (80 rows). Neither is a proven defect: the applicant and the lawful bottler may differ, the tool has no table of permitted designations, and a line the engine did not read is a heuristic miss. The rule in AGENTS.md is that heuristic findings are Needs review, never Fail, and only clear text or numeric differences fail; the reviewer's consult (docs/reviews/006) reached the same matrix. The blanket "not found is an issue" rule was the brief's synthetic sample talking. | `docs/reviews/006-registry-fields.*`; `tests/unit/test_parsers.py` (bottler four ways, class, alcohol, origin), `tests/unit/test_normalize.py` (registered line parsing); `docs/EVAL_REAL.md`. |
